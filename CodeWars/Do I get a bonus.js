function bonusTime(salary, bonus) {
    // your code here
      if (bonus == true) {
        return "?" + (salary *= 10);
      } else if (bonus == false) {
        return "?" + salary;
      }
    }
    